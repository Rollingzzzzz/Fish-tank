# TRACEABILITY — requirement → goal → evidence (G0 rule, v0.2)

Append-only. Every product requirement maps to a goal ID on the README §10 board.
Evidence = commit hash, test name, PNG path, or live-run screenshot. **A goal without evidence
blocks the release.**

| # | Requirement | Goal | Evidence |
|---|------------------------------------------|------|----------|
| 1 | Plant stems look triangular — must be smooth; keep the sway | F1 | 6208591+2168661: arc-length resampling + bowtie fix; docs/art/v02/plants-smooth.png + stem-zoom2.png (smooth) |
| 2 | Too much neon — must not strain the eyes | F2 | d422672: GlowBudget 0.55; pngstats day-noon hot>240 = 0.061% (≤2%) |
| 3 | Fish should head for food faster | F3 | d422672: TestFoodRushCovers300pxFast PASS |
| 4 | Richer interactions: mutual fright, chases, random weird moves | F4 | d422672: TestBehaviorPackSoak PASS (startles/chases/zoomies/nudges) |
| 5 | Agent menu does not work | F5 | 2168661: SMOKE OK (menu first-click, 5 tabs, tray, grab→drop) |
| 6 | Breeding / egg-laying never happens | F6 | d422672: TestBreedingSoakWithAutoCare + TestNoBreedingWithoutCare PASS |
| 7 | Aging with gradual color fade into old age (stated in v0.1, missed) | F7 | d422672: TestElderFadeProgressMonotonic + TestChosenNeverAgesOrDies PASS; elder-fade-pair.png |
| 8 | Top-right HUD: game time + day count | F8 | 6208591: ui.Chip HUD + world.TimeOfDay; live screenshot 'NIGHT - DAY 52' chip legible|
| 9 | Morning vs evening must differ; a sense of days passing | F9 | 2168661: daylut tests PASS; day-0.02/0.25/0.5/0.75.png distinct |
| 10 | No sound effects at all — they distract | F10 | 6208591: zero SFX call sites; engine music-only |
| 11 | Fish count never below a floor; good feeding → more births, fewer deaths; auto-feed maintains the current population; manual feeding still works | F11 | d422672: TestPopulationFloorSelfHeals + TestMaintenanceAutoFeedHoldsBand PASS |
| 12 | Corals: beautiful, water-moving, rive-loop feel | N1 | 1412982: corals end-to-end + 23 coral files (4 seed + 19 agent-produced); live run corals 6; coral-rock-scene.png|
| 13 | Volcanic rocks fish can enter | N2 | TestStartledFishSheltersInCave PASS; live screenshot: elder sheltering inside left cave; coral-rock-scene.png|
| 14 | One special lilac fish: exclusive color, grander, bigger, faster, immortal; oyster nest with 3–4 pearls; invisible circle only it may enter | N3 | d422672: TestChosenAuraExclusion + TestChosenAlwaysRestored PASS; chosen-lilastar seed; shimmer render |
| 15 | Crustaceans; fish attack them, they retreat into shells; random comedy events | N4 | d422672: TestCrustaceansSpawnAndRetreat PASS (feature retired in v0.3.8 — see row 52) |
| 16 | Glass-sucking (pleco) fish stuck to the glass — realistic, odd | N5 | d422672: TestPlecoAttachesToGlass PASS |
| 17 | Piano-led ambient music, underwater feel, calm loop, no distractions (original, royalty-free) | N6 | Lane C: music tests PASS; -15.78 dBFS RMS peak 0.53; demo-music -stats |
| 18 | Plants must not carpet the tank — ecosystem balance | N7 | d422672: TestMitesSpawnAndFrenzy PASS; spacingOK rule |
| 19 | Wild water mites appear on their own; fish love eating them, they scramble | N7 | d422672: TestMitesSpawnAndFrenzy PASS; spacingOK rule |
| 20 | Top-right treat store: grab live bug / earthworm / shrimp / chicken with the mouse, drop in, fish swarm; palette-true rive-quality | N8 | SMOKE OK grab→drop; treats tests PASS; tray tests PASS |
| 21 | Use subagents for speed at the implementer's discretion | waves |  |
| 22 | Everything tested thoroughly before stopping | DoD |  |

---

# v0.3 — WIDE GLASS (requirements → goals → evidence)

| # | Requirement | Goal | Evidence |
|---|------------------------------------------|------|----------|
| 23 | Menu buttons are not visible — all must be visible; verify with screenshots until fixed | F12 | 5f9ac3e+W2: shared ui/geometry.go; rect-based font (labels 255 lum, was ~139); uiaudit 336/336 PASS over shot-16x9/1610/32/219; live zoom read AGENTS/SPECIES/PLANTS/LOG/SETUP + Auto feed row |
| 24 | App must open fullscreen; proportional scale on any monitor/window size | F13 | 87fd062: SetFullscreen on start + LogicalWidthFor clamps; 4-aspect evidence matrix (1280/1152/1080/1720 × 720) — no bars, no off-frame controls; live window bounds 0,0,3440,1440 (21:9 monitor) |
| 25 | Fish count must always exceed plant count | F14 | Lane C: TestFishAlwaysOutnumberPlantsSoak/TestAddPlantAboveEffectiveCapIsNoOp/TestDeathCullsOldestPlant PASS; live debug: fish 17 plants 5 |
| 26 | Volcanic rocks need holes; fish should occasionally enjoy entering them | F15 | Lane C: TestForcedLoungeHoldsCave + TestLoungeFrequencySoak PASS; Lane B: indigo+rim-light cave regrade |
| 27 | More species variety; none as big/beautiful as the lilac; ONLY the lilac has neon | N9+F16 | Lane D: 3 new seeds + role-aware clamps + TestSeedSpeciesSupremacy; Lane B: TestGlowVisible/TestPixelAcceptance (0 glow px for normals); n9-gallery.png Chosen 297×180 vs next 243×135 |
| 28 | Turn off unnecessary plant/flower glow; keep the lovely waves | F17 | Lane B: f17-plants-before/after pngstats hot 3.65%→0.013%; sway code paths untouched |
| 29 | Weird blackness on screen; if it's the sucker: mouth must move, belly visible, slower than others | F18 | darkscan: no ≥300×80 lum<14 region outside graded caves; f18-sucker-1/2.png (mouth aperture pumps, hot 0.31%→0.89%); sucker pose: flat + pale belly band; speed 0.55→0.42 |
| 30 | Right-click to scare fish; they bolt then forget, like tapping glass | N10 | Lane C: TestRightClickScareBoltsAndFades PASS; -menu-smoke scare-wired step (no feeding on right click) |
| 31 | Power-up treats: wriggle at the cursor like bait on a hook; fish crave and scramble for them | N11 | TestHeldTreatPullsHungryFishToRing + TestTreatScrambleManyFishDevourWorm (3 distinct biters, 4/4 bites) PASS; LIVE: hold (S-curl worm + 6–8 open-mouthed fish) → drop → 8–9 fish frenzy with bite sparkles → debug treats 0 |
| 32 | (v0.2 leftover) treats/creatures/mites were invisible no-op stubs | N11+F22 | render.DrawTreat/DrawMite implemented (DrawCreature retired in v0.3.8 — see row 52); f22-decor.png; render pixel tests; live worm visible |
| 33 | The first music pass did not deliver the intended calm piano feel — replace it with a proper original piece | N12 | Lane D: original reference-evoking score — TestAuthoredEventCount, TestOnsetSharpness, seam delta, loudness gate PASS; docs/art/v03/n12-music.wav (superseded by docs/art/v038/n12-music-v3.wav) |
| 34 | Auto feed on/off button must be visible in the menu — test it; auto-feed must preserve current fish count | F12+F11 | LIVE: Setup tab row read + toggle flipped ON by real mouse (zoom screenshots); manifest rect 'settings-Auto feed' uiaudit PASS; TestAutoFeedPreservesPopulation (5 virtual days, 8→8 alive, flakes ≤10) + TestMaintenanceAutoFeedHoldsBand PASS |
| 35 | Everything in the plan — including past goals — done, working, in harmony as one exe | G0/DoD | full suite 11 pkgs ok; scripts/check incl. evidence gate; -menu-smoke SMOKE OK; uiaudit 336/336; darkscan clean; live exe run verified all of the above |

| # | Requirement | Goal | Evidence |
|---|------------------------------------------|------|----------|
| 36 | Fullscreen fish look too big; prefer more fish/corals/rocks/detail — treat screen size as tank size; fix pixelation on big screens | v0.3.1 | 94b5426→v0.3.1: logical canvas = physical monitor resolution (natural fish size, pixel-perfect); area-scaled density (fish ≤56, plants ≤30, creatures ≤10, mites ≤16, coral pitch, per-species start school, rock k-scale); TestBigTankIsABiggerAquarium/TestPlantCapScalesButFishStillOutnumber/TestRestoreRescalesOldPositions PASS; live 3440×1440: 47 fish, 23 plants, 60/60 FPS, sharp edges; shot-219-big uiaudit PASS; live-fullscreen-v031.png |
| 37 | Plants claim too much floor — wanted floor split: ~20% plants, 40% volcanic rock (enterable), 20% corals+props, 20% open water | v0.3.2 | f8f0096→v0.3.2: FloorShare* contract constants; plant/coral budgets floor-share based; rock area 40%W (caves 15+15, ledge 8, nest 2), mouths scaled and enterable; TestFloorCompositionSplit PASS (1280/1720/3440: 20/40/20/20-21%); -shot debug + visual QA confirmed the same split; restore prunes legacy plant excess |
| 38 | A close button is mandatory; the daytime clock belongs top-left (it collided with the menu) | v0.3.3 | UIMetrics.CloseBtn + drawClose (red-framed X, above the menu); click → shutdown + Termination (ESC fallback); HUD chip top-left via ChipLeft; LIVE: X closed in one click (tasklist empty), ESC closed, top-left 'DAY 74 - 06:24' read; uiaudit close-button PASS on all frames; TestCloseButtonGeometry PASS |
| 39 | The lilac fish needs a centerpiece home: an oyster 5-6× her size, camera-facing and open, exactly 3 animated pearls, a living tongue-like texture, contact interactions; the Chosen is immortal and ONE — not spawnable from menus | v0.3.5 | nest.go: centered Nest.X=W/2 (test), ~470px camera-facing open oyster (cup + nacre lid + breathing mantle + satin tongue), pearls=3 (test), contact scene (pearl cross-flare, tongue reaches, lid opens, lilac motes rise) — Chosen verified inside the gape in a live PNG; catalogList RoleChosen filter (TestCatalogExcludesChosen) + SpawnEgg guard (TestChosenCannotBeSpawned); uiaudit/test/check green |
| 40 | The lilac fish must be the single one, larger than the rest, immortal, fastest of all; neon glow hers alone | v0.3.6 F23 | role-aware speed clamp NormalSpeedMax=1.35 (validate+generator+seed, dart-spindle 1.30) + ChosenSpeedMul=1.45 frozen; TestChosenFastestAtAllHours (night 0/0.5/1 × all stages, ≥15% margin), TestCullNeverTakesTheChosen, TestRestoreDedupesChosen (a duplicate lilac dissolves), TestRestoredElderChosenStaysPrime (ElderP=0, adult speed); glow F16 gate already role-gated; live: single largest/brightest lilac visually confirmed |
| 41 | The oyster does not read as an oyster yet; it must lean against the very bottom of the window | v0.3.6 F24 | NestBaseY=H (±4px, tested at 3 canvas sizes: TestNestFlushBottomAndMouthPlane) + full anatomy: two fluted asymmetric valves, hinge heel, tilted lid, curling satin tongue, exactly 3 pearls in the gape (TestNestDeadCenterWithThreePearls, nestFlushPixelProof); live: 'shell extends to the bottom edge, no gap' confirmed (re-seated to the shared floor line in v0.3.8 — see row 49) |
| 42 | No volcanic rocks yet; they should claim real floor area with doors/holes — a fish enters one hole and exits from ANOTHER | v0.3.6 F25 | twin jagged basalt ridges (peaks ≥0.40H, rocks 50% floor share), 3 mouths per ridge (collar + sill); TestVolcanicCragLayout (4 seeds × 2 sizes, 6 holes) + doorMouthPixelProof; transit: TestForcedTransitThroughCrag (hidden ≤2s after entry, ≤90px from the EXIT door and far from entry, exit speed ≥1.5× cruise), TestTransitFrequencySoak (≥1 in 10min), TestScareAbortsApproach; PNGs 11-12-13 (at the mouth → inside → bursting from another door with bubbles); live: 2-3 doors read per ridge; the Chosen never transits |
| 43 | Clicking a treat must SELECT it; it stays held until the next left click; it follows the cursor; fish smell it but cannot eat it until dropped | v0.3.6 F26 | row PRESS grabs (tray closes), carry continues button-UP: TestCarryToggleMenuClosed (≥60 frames, second press drops exactly there, no FeedAt that frame), TestCarrySwallowsCloseButton (drops on the X press without quitting), scent growth HeldScentGrowPx=20/cap140 (TestHeldTreatScentGrowsWithCarryTime: fish at 350px arrive ≤6s); smoke carry block 6/6; LIVE: click-select → tray closed → worm followed the cursor (button up) → fish gathered but could NOT eat → second click → dropped exactly there → frenzy (docs/art/v036-live/live-02/03) |
| 44 | A shapeless triangle inside the oyster; outer shell pieces disjoint; add silky hair-like strands; blend it into the background; only the lilac fish may enter | v0.3.7 F27 | root cause: folding tongue segments (0.92 wedge gaps) + flat ligament slab → watertight tongue strip (shared edge), ligament soft shadow; lid lip lands on the cup shoulders (TestNestOneShellGeometry: center over the mouth, ends on the shoulders); byssusStrands ≥6 threads hinge→floor; AO ambient pocket + ground shadow; sim: aura repulsion exempts the Chosen (TestNestBelongsToTheChosen: normals never enter, she roams free inside); PNGs 01/10 close-up visual pass |
| 45 | The menu must open below TREATS and the close button, never over them | v0.3.7 F28 | UIMetrics.PanelTopY = below TrayBtn +2 (TrayPanel ideology); GlassPanel/clip/global tab drawing from panelTop; Consumes now carries a y-bound; TestMenuClearsTopChrome (3 widths: panel top ≥ lowest chrome bottom; TREATS/X clicks reach their buttons); smoke 6/6 tab steps; uiaudit tab-* 674/674; PNGs 02-06 tab bar under TREATS |
| 46 | Allow the max fish count to go up to 100 | v0.3.7 F29 | config clamp 8..100 + slider max 100 + PopCapMax 56→100 (frozen + TestTuningFrozen); TestPopCapHonorsUserMax (720p popCap=100, big-tank cap=100, hatch gate stops at 100); live: locked 60 TPS at 100 live fish, 17 FPS (GPU-bound — measured note in README, 4 FPS before FishBatch); 15-crowd.png (alive=100); profile: 86% cgocall → FishBatch single draw (~2 calls/school) |
| 47 | Add silky, feathery, strange corals/plants — more variety | v0.3.7 F30 | PlantDesign.Kind ('ribbon' default/legacy clamp, 'feather'+'silk' new); plant_feather.go: sea-pen barbs (18 pairs) + silk threads (3 passes); 6 embedded seeds (TestSilkySeedsShipInStore; store 4→10 plants); TestPlantKindClamp; 14-flora.png gallery (≥2 feather + ≥2 silk, matte — F17 kept); demo-agents count updated |
| 48 | The door-hide effect is not fully working; grow the rock houses | v0.3.7 F31 | root cause: the crag body was drawn BEHIND the fish (alpha fade insufficient) → Hide01>0 fish drawn in the pre-DrawBack pass (two-pass); PNGs 11→12→13: the vanish at the mouth is instant; crags grew: bw ≥0.26W, peaks ≥0.46H, mouths +15%, inner edge 0.31/0.69W band (TestVolcanicCragLayout updated); TestForcedTransitThroughCrag green; FishBatch: one draw per school (superseded by the binary swallow — see row 50) |
| 49 | The lilac fish's nest stands apart from the aquarium and chokes the view; rework it to read 2D yet give a 3D feel — placed like a stage set with plants behind it for depth | v0.3.8 G32 | oysterSize 470→360px / ≤30%W cap (TestNestSceneryScale, 3 sizes); NestBaseY H→0.925H shared floor line (TestNestFloorLineAndMouthPlane); DrawNest moved after DrawCorals → plants render behind the shell, the WHOLE school (and she) in front; ambient dome alpha 185→80, gape lifted two steps, halo halved; darkscan 01-closed: biggest dark region 7985px² (the oyster blob) → 495px²; nestFloorPixelProof seats it on the floor line; 01-closed.png: planted-throne composition |
| 50 | Fish entering the volcanic houses show through as transparent — breaks the realism | v0.3.8 G33 | root cause: the TransitHideSec 0.25s alpha ramp (partial alpha over open water during the pre-rock pass) → binary swallow: Hide01 ∈ {0,1}, instant 1 at the mouth plane, instant 0 once clear of the far mouth; the dead pre-rock fish pass deleted; TransitHideSec const retired (TestTuningFrozen updated); TestForcedTransitThroughCrag green (hides instantly, exit ≥1.5× cruise); PNGs 11→12→13: opaque → gone → opaque emergence |
| 51 | Fish should roam the aquarium more; they keep gathering around the rock towers | v0.3.8 G34 | roam.go: tourSteer (random full-tank waypoint every 5-9s, weight 0.9; suspended while scared/lounging/transiting/in-nest); lounge: one fish per cave + ≤20% of the school, mean 45→120s, dwell 8-18→5-10s; energy drain -40% (0.010+0.020→0.006+0.012); cohesion excludes lounge/transit fish + weight 1.15→0.95; scareT=8s: the shelter pull outlives the startle until cover is reached (TestStartledFishSheltersInCave); a full tank stops breeding (hatch→cull→fade churn cut; Snapshot skips dying fish); NEW -shot probe cluster%: baseline 57-85% (mid-run) / 21% (final) → 6% final; 15-crowd.png: school spread across the whole column |
| 52 | Remove the black U-shaped critters (crabs) at the left and right | v0.3.8 G35 | N4 crustaceans (2 crabs + 2 shrimp, ~17px dark domes reading as black U's on the night floor) retired: spawn/tick/draw + DrawCreature + CreatureCount/CreatureCapMax consts + the lane_b test block deleted; SavedCreature struct + Save.Creatures field stay for save compatibility (parsed, ignored); mites stay (TestMitesSpawnAndFrenzy); demo-decor gallery updated to mite phases; floor clean in all PNGs |
| 53 | Bold visual pass: compose the tank like a stage (2D with a 3D feel) | v0.3.8 G36 | ForegroundStage: 3-4 large cool-dim fronds (dimCoolRGBA k=0.38, blue bias) rooted at the bottom edge at the flanks (x ≤0.12W / ≥0.88W, center open); drawn after rock.DrawFront → 3 planes: dark crags → midground flora+school+nest → foreground glass silhouettes; PERF: per-frond mesh draws profiled badly (+45% draw calls) → each frond BAKED once offscreen, 1 DrawImage per frame (±0.7° whole-plant breathing); interleaved TPS @100 fish: v0.3.8 54-56 vs v0.3.7 57 (parity); 01-closed.png flank silhouettes frame the scene |
| 54 | The reference-style piano loop is not close enough — it should evoke the original more | v0.3.8.1 G37 | root finding: the harmony language was already right (Imaj9 + borrowed iv + chromatic lift); the TEXTURE was wrong — sparse half-note LH + straight-16th melody → LH rewritten as the signature 12/8 triplet broken-chord wave (12 notes/bar, low root→fifth→tenth, sustain-pedal tails), melody re-rhythm'd onto the triplet lilt (x.333/x.667 pickups, long phrase ends, bar-20 exhale); 75 BPM/20 bars/64.000s kept; the melody stays FULLY ORIGINAL; TestAuthoredEventCount 340 (73+234+33), grid test moved to the 1/12-beat grid (worst 11.4ms), TestOnsetSharpness tiered (accents ≥4×, all ≥2.2×; min 2.83/median 5.28); loudness -17.72 dBFS, seam 0.3× median; listening evidence docs/art/v038/n12-music-v3.wav (cmd/demo-music -wav) |
| 55 | Ship the full evocation package: tempo lift, warm-tenth voicing, celesta layer, dynamic arc | v0.3.8.2 G38 | tempo 75→90 BPM (24 bars; beat = exactly 14700 samples, 64.000s kept — TestRenderLength), B flight stretched to 10 bars (Fm6→G13 two-bar turnaround), LH wave ascends through the warm tenth (low root→root→tenth→fifth crest), +24 celesta halo on the big B notes (VoiceCelesta, ×0.10), arcMult dynamic arc (A' +3%, B 97→106%, A'' 95→85%), ritardS seam ritardando (from beat 84, ≤36 ms — grid tolerance 50ms documented); the melody remains FULLY ORIGINAL; TestAuthoredEventCount 421 (84+282+42+13); TestTempoGrid12ths worst 9.3ms; TestOnsetSharpness min 2.40/median 4.89/max 13.13 (accents ≥4×, all ≥2.2×); -17.62 dBFS, peak 0.88, seam 0.1× median; listening evidence docs/art/v038/n12-music-v3.wav; MusicBPM const 75→90 (not test-frozen), demo-music grid formula updated |
| 56 | Giant slow fish visit the tank as a pack of 2–5 with exactly one vast leader, cruising ponderously, present only as visits | v1.1 G39 | TestTitanPodBoundsAndVisitCycle (pod 2–5, one giant, exit clears the tank, next gap 180–360s); TestTitanPonderousCruise (cruise in the slow band, far below a normal); 11-titan.png pod on screen |
| 57 | The largest one fills about a tenth of the aquarium | v1.1 G40 | docs/art/v11-probe/14-titan.png: rendered silhouette bbox 436×316 = 137,776 px² = 11.1% of the 1720×720 canvas (HSV-masked measurement; Size 8.8 core seed → 64×8.8 ≈ 563 px spine) |
| 58 | When hungry, a giant suddenly dives — a shock lunge | v1.1 G41 | TestTitanHungerLungeBurst: seekBonus ×TitanLungeMul during the burst, measured speed ≥6× cruise; per-fish cooldown 90 s (TitanLungeCD); flakes/treats are the target, a bare dart when the tank is empty |
| 59 | A giant that starves for a long time takes one small fish — rare and guarded | v1.1 G42 | TestPredationGate: fed giant never hunts; PredationSustain 20 s + tank cooldown 300 s + population floor (MinPopulation+2); swallow = fast fade, no corpse, one log line; the Chosen is immune |
| 60 | The giants are not selectable from the menu anywhere | v1.1 G43 | TestCatalogExcludesTitan (catalog filter); SpawnEgg guard (TestTitanAmbientExclusions); validate.go rejects any non-core titan role (TestTitanRoleCoreOnly) — agents/packs/users cannot mint one; NewWorld never seeds the pod |
| 61 | Crabs and shrimp walk the tank floor, spread across the sand | v1.1 G44 | TestCritterCapSpreadAndNestExclusion (area cap 8×density ≤14, sand band, spread); 10-critters.png / amber-pixel scan x=96..1334; TestCritterBurrowExit (uneaten → sinks back, no corpse) |
| 62 | Near the end of their cycle they lure hungry fish and get eaten | v1.1 G45 | TestCritterLureSteersHungryFish (struggle-phase pull within 260 px; full fish indifferent); TestCritterIsEatenWhenReached (eat+care+log; hard-shell walk not edible) |
| 63 | The sand circle belongs to the lilac fish alone; the new life must stay visible day and night | v1.1 G46 | zero-intrusion assertion over a full cap cycle + enforceZonesPos per tick + spawn rim check (steerTitan keeps the pod off the aura too); visibility: darkscan probe PNGs — largest dark region 475 px² (no v0.3.8 clutter); amber-vs-floor luminance day 1.96–2.57×, night 1.92–2.89× |
| 64 | The additions must not cost frames | v1.1 G47 | interleaved A/B on the same save (TANK_EXTRAS=0/1, -probe): capture TPS OFF 55–60 vs ON 58–60, exit rates OFF 54/55 vs ON 57/57 — parity within machine noise (GPU-bound, FPS 11–13); fresh-tank default run 60/60/59 TPS |
| 65 | The giants must wear vivid, lively colors — no dull dark murk, nothing brown or dreary | v1.1 G48 | titan-abyssdrifter livery: cerulean #1e9fd8 + ice #b8f1ff + sun-gold #ffd23f + electric-cyan #45e0ff (TestTitanRoleCoreOnly freezes the palette; the reserved lilac band is still refused); floor critters recolored amber→coral #ff9f43/#ffd166/#e0631f — luminance vs floor re-measured day 1.81–1.89×, night 1.76–1.90×; docs/art/v11-probe/*.png |
| 66 | The giants must startle at right-click like the rest of the tank | v1.1 G49 | flee records the scare point; steerTitan bolts away from it at TitanLungeMul, decaying over the 8 s startle window (the ScareRadius input path already covers every fish incl. titans); TestTitanScareBolt (≥6× cruise, distance from the point grows); the same pass delivers the pod's spine curvature clamp (TitanSpineBend 0.085 rad/segment, TestTitanSpineNeverFolds) — the giant keeps its ~576 px silhouette through lunges and startles; straight-swim livery ≈550×250 px ≈11% of canvas, full envelope with fins ≈15.8% (13-titan.png, 16-titan.png; supersedes theSize-8.8 frame cited in row 57) |
| 67 | A hammerhead shark: dark blue-black with a white belly, active and roaming, at most two of them, and never faster than the lilac fish | v1.1 G50 | shark-hammerhead core seed (body #101c2e, belly #f2f6fa, steel accent; Size 3.5 ≈ 224 px; TestSeedSpeciesSupremacy shark branch); resident pair kept whole by ensureSharks at SharkMax=2 (TestSharkPairCap); pace supremacy at every hour incl. a frenzy double (TestSharkRoamsBelowHer — same-hour comparison per F23); roam steering carries the pair across the tank (30 s displacement asserted); off the menu and eggs (catalog filter + SpawnEgg guard); pair persists in snapshots, capped on restore (TestSharkExclusionsAndPersistence); the hammer rostrum renders as a crossbar with tip eyes (render/fish.go) and the pod's spine curvature clamp covers the shark (13/14-titan.png) |
| 68 | Small fish should sometimes cross in front of the big ones and sometimes behind — a 3D read; they must never swim through a big body but flow around it; and the giants must make wide crosswise turns, bending as they reverse direction | v1.1 G51 | depth lanes: every small fish drifts between the near and far water (DepthSwing ±0.35, TestDepthLanesDrift — band + drift asserted; the big fish hold the mid lane) and the school renders in lane order (game_draw painter's sort + lane size/alpha cues in render), so the giants are crossed in front or shadowed behind; big-body avoidance: small fish steer around titan/shark silhouettes within 0.55×body length (TestSmallFishFlowsAroundBigBodies); crosswise turns: the pod re-aims its heading by up to ≈180° every ~25 s mean (TestTitanCrossTurns — ≥2 wide swings in 150 s) with the curvature clamp rendering the bend |
| 69 | Big fish must not slip off the frame and stay lost; they should favor the upper 80% of the water and swim parallel to the sand line, turning across it rarely rather than darting up or down; and as a fish grows its daily movement heavies while the capture burst gets longer — one tail whip covers real distance | v1.1 G52 | edge recovery: anything roaming past the frame margin steers back into view (scare stands down off-screen so the two forces cannot cancel — TestTitanEdgeRecovery: worst off-screen streak ≤10 s across 120 s of repeated edge scares; phase-3 exits boosted ×2.5 and never pinned); upper-band + sand-line headings: TitanUpperBand 0.78H ceiling + wander vertical squashed (TitanHeadFlat) + level-swimming damping + band pull with a wide dead zone (TestTitanFavorsUpperWaterAndHorizon: <5% deep time, vertical travel share ≤30%); growth weights: pod cruise ×(1.12−0.22·sizeMul), capture burst ×(0.70+0.30·sizeMul), tail beat damped 64/(64+0.35·bodyLen) (TestBeatSlowsWithGrowth); shark tour waypoints confined to the upper water |
| 70 | The giants should be big and FLAT — a wide oval, not vertically tall | v1.1 G53 | TitanFlatness 0.62 flattens the body profile and dorsal/pectoral fins for the titan role (render/fish.go); TitanSizeMax 9→10.5 with the seed at 10.0 (spine ≈640 px) so the length carries the size; measured straight-swim livery 583×194 (aspect 0.33, ≈9.1% of canvas — docs/art/v11-probe/11-titan.png; supersedes the taller G40 pose, the fill-presence now rides the length and the sweep) |
| 71 | A yellowish-white grain bed on the floor, reaching up to the lilac fish's nest boundary; fish swimming close scatter the grains, and the bed re-levels naturally afterwards | v1.1 G54 | sim/sand.go: one disturbance cell per SandCellPx, kicked by fish skimming the floor (strength ∝ speed), sideways scatter + diffusion + settle damping back to flat (TestSandStirsAndSettles: disturbed then calm within 45 s; TestSandStaysBounded: offsets stay in [-4,10]); render/sand.go: 5 deterministic speckles per cell in pale gold/cream (#f6f0dc/#ecd9a0/#d4c492), lift+sideways push from the offsets, nest circle skipped on the floor line (sandKeepClear — grains reach up to her boundary and stop); never persisted, TPS unchanged at 56 in the probe run |
| 72 | Big fish were occupying the lilac fish's nest boundary — absolutely unacceptable: no creature may enter that circle, and "enter" must mean the WHOLE body, not just a fish's head | v1.1 G55 | enforceFishZones (sim/decor.go): the deepest spine segment decides the projection — the head clears to R+16 at once on fresh data, the rest of the body drains out at ≤12 px per frame, inward velocity is killed, and +16 clears inter-frame drift (TestBigBodiesNeverEnterTheCircle: a giant parked straight across the circle resolves out within 3 s and the closest spine approach stays ≥ R for the next 27 s; the same law covers sharks, escorts and the school — critters/mites project with a +12 margin); early repulsion is body-aware in both steering mixes (R+40+45 % of body length, ×1.8/×2.0 weight) so big bodies turn away half a body-length before the line; TestNestBelongsToTheChosen pinned to deterministic wander (the v1 rng stream shifted with newFish's lane draws) |
| 73 | Some school fish look sickly — a dull slate-gray-blue with a washed pale belly; recolor them vivid | v1.1 G56 | root cause: the glass-sucker core seed wore body #5f7d8c / belly #8fb3b6 (slate-on-pale-gray reads diseased, and it schools + glass-attaches so several sit at one edge); recolored to rose #ff5f9f body, cream #ffe3ef belly, gold #ffd23f accent (seed frozen by content tests via the frozen-palette rule; runtime copy re-seeded); depth-lane dimming floor softened 0.84→0.88 so far-lane fish stay lively (DepthNearAlpha); probe frame 13-titan.png — no slate fish remain |
| 74 | The sand gap under the nest makes the oyster look like it is floating — fill the floor continuously, the bed must not stop at the nest | v1.1 G57 | DrawSandBed lost the keep-clear span: the grain line runs the full floor width and the nest pedestal simply covers its own stretch (render/sand.go signature dropped clearX0/clearX1; sandKeepClear helper removed); 14-titan.png: one continuous sand line under the whole floor, no floating-nest gap; supersedes the bare-circle half of row 71 (the circle law of G55/G46 is about creatures, not grains) |
| 75 | The big flat fish should be SCALARE (angelfish) like the reference photo: groups of five with one biggest leader, sweeping the tank left-to-right and right-to-left, able to move only toward their head, and at the tank's far end curling 180° gracefully around their own axis; rare hungry bursts stay | v1.1 G58 | TitanTallPeak 0.42 turns the scalare body into a tall flat diamond (render/fish.go peakW override) with towering dorsal + a mirrored anal fin (finK 1.6) and two trailing ventral streamers; eye capped at 5% of body length; the pod travels as a fixed five (TitanPodMin/Max 5/5, 1 leader sizeMul 1.0 + 4 escorts); sweep steering: members cruise toward one glass and at TitanEdgeTurn 0.12 curl 180° in TitanTurnWindow 2.6 s with the spine bend relaxed to TitanSpineBendTurn 0.22 (followSpine dynamic bend; TestTitanCrossTurns ≥1 wide swing per 150 s; TestTitanSpineNeverFolds still green — the curl may use the full arc but never folds); lunge bursts unchanged (rare hungry dashes); livery silver-pearl #d9e2ea with dark slate stripes #37475a per the reference photo (TestTitanRoleCoreOnly frozen palette); supersedes the flat-oval geometry of row 70 |
| 76 | The scalare sometimes drifted tail-first during the curl and the tail folded onto the body — both must never happen: the curl is carved forward and the tail stays clear | v1.1 G59 | curl carve: while turning>0 the speed is floored at 0.5× cruise along the travel direction (advance carve; TestTitanCurlForwardOnly — min speed ≥0.35× cruise through the curl); the turn's spine bend tightened 0.22→0.18 so the tail cannot fold back onto the body (TestTitanSpineNeverFolds); formation: the pod holds staggered slots behind the leader (slotBack×cruise, slotY) and sweeps as one — probe 14-titan.png: the five in a diagonal parallel line with the school flowing around them; big residents drift the far depth lane (0.30±0.12) so the school reads in front — TestDepthLanesDrift far-band assert |
| 77 | The sand must be a FULL layer down to the bottom edge — the speckle line left the lower band transparent and the nest read as floating in the air | v1.1 G60 | DrawSandBed now paints a solid quad from the floor line (0.925H) to the bottom edge, full width, warm sand #e9d9ab deepening to #c9b483, alpha ~245 (the background never shows through); the speckle clusters ride on top spread across the upper layer; the layer dims with the scene at night but stays readable; probe 14-titan.png: continuous filled band, no gap under the nest |
| 78 | The sand band must be the FRONT-most layer — nothing but sand may show in it; every other object (crags, flora, rock faces, fg fronds) stays behind, and the bed must occlude fully at night too | v1.1 G61 | compositing order moved: game_draw.go now draws the bed after rock.DrawFront + fg flora (far crags → school/nest → fg fronds → SAND → floor dwellers); critters, mites and sinking food stay on top of the bed (they live on its surface); night dimming switched alpha→color — the old alpha 245×(1−0.55·night) went translucent and let the scene bleed through at night, now the palette lerps toward cSandNight #56524a at constant alpha 245 (render/sand.go); probe 14-titan.png (day) + 12-night.png (night): the band shows only sand, grains and critters; supersedes the dim wording of row 77 |
| 79 | The hammerhead may swim ONLY toward its head — live review caught it gliding tail-first when opposing forces (school, scare, rear waypoints) outweighed the forward pull | v1.1 G62 | advance() now calls constrainForward for RoleShark: the velocity is rebuilt along the body axis every frame and the axis itself swings at most SharkTurnRate 2.8 rad/s, so opposing forces carve the shark around instead of sliding it backward; a 0.65×maxSp floor keeps the hunter under way through the turn (G50 roam distance held); heading is seeded at spawn (facing open water) and rebuilt from restored velocity on load (it was never persisted); TestSharkSwimsOnlyTowardItsHead (rear-pinned waypoint for 30 s: windowed net motion never falls backward of the head, and the shark carves >126° around) + TestSharkRestoreFacesItsMotion; titan curl carve extracted to titan.go carveCurlTurn for the line ceiling |
| 80 | No fish may swim tail-first — every body must move from tail toward eyes, the eye always leading; tail fins must sit ON the body (never whirl like clock hands); a spine may arc like a rope but never coil onto itself. Verify by letting time run over the tank and checking each eye travels forward | v1.1 G63 | root cause found one sign deep: the G58 curvature clamp referenced the FORWARD swim axis, so every correctly-trailing segment violated it and was re-laid AHEAD of the head — titans/sharks (clamped since G58) and then the whole school rendered end-for-end inverted (eyes trailing, caudal fins plowing like propellers); followSpine now hangs the chain from the TRAILING axis (−velocity, or the existing hang at a standstill) with per-species caps — TitanSpineBend 0.085/0.18 curl, SpineBendNormal 0.50 — a rope may arc, never coil; the generic-mix forward law is capTurn (NormalTurnRate 5.0 rad/s: a rear target bends the path into an arc, no flip) with heading rebuilt from saved velocity on restore; hammerhead keeps its stricter axis pin (G62); TestTimeLapseEveryEyeLeadsItsMotion — 90 s over a mixed tank (school + Chosen + pod + pair), 8009 windows: per-frame dot(velocity, eye-axis) never backward, worst spine bend 0.68 rad; probe 11-titan.png: scalare eyes lead the sweep, hammer+eyes lead the shark; closes the v1 known issue (README) |
| 81 | Some background plants — especially the corner ones — sway pixelated, freeze-stepping like low fps next to the smooth plants; fix it | v1.1 G64 | the corner near-glass fronds are baked rasters composited with a per-frame ±0.7° breathe rotation, and ebiten DrawImage defaults to nearest-neighbor sampling: the rotation stepped texel by texel while the live vector plants drew through anti-aliased triangles — end-for-end different smoothness on the same screen; ForegroundStage now composites through breatheOptions with FilterLinear so the rotation interpolates smoothly; TestForegroundBreatheSamplesSmoothly pins the filter choice and the transform; probe 11-titan.png corner crop: soft continuous strokes, no stair-stepping; TPS 58 unchanged (4 DrawImage calls per frame) |
| 82 | The movement fixes introduced glitching — some fish move in teleport-like jumps; no fish may ever glitch-teleport | v1.1 G65 | the G62/G63 motion pins discard the lateral part of avoidance, so fish now reach the Chosen rim still driving forward and the enforcer answered with a radial SNAP all the way out to need = R+16+half-body (up to ~73 px for a shark, ~31 px for a neon) — a visible teleport; enforceFishZones reworked: a head found inside lands ON the rim (R+2, a solid-wall projection bounded by the frame own travel), the sagging-body drain halved 12→6 px per frame, and the pin heading slides onto the outward hemisphere so the fish skirts the rim instead of grinding back in; TestNoFishTeleportsAtTheNestRim launches a shark and a normal straight at the circle for 8 s — every per-frame jump stays within 3× the fish own speed budget + 8 px (the Chosen burst passes, the old snap fails), and her head never rests inside the ring |
| 83 | No part of the big bodies may ever leave the visible area | v1.1 G66 | the titan pod carried a v1.1 freeEdge exemption (visit-machine leftover: titans skipped the X bounds entirely) and the tall scalare frames clipped through the top edge; applyFrameBounds now contains every fish (freeEdge removed) with a body-scaled top margin (0.42×bodyLen+6, covering the trailing cone dive excursion) and a body-relative ceiling force; a body-scaled curl trigger (0.5×bodyLen+24 px from the glass, superseding TitanEdgeTurn 0.12W) gives the 180° arc room to turn fully inside; clampBodyInFrame runs as the last spine pass — a per-point canvas clamp whose rare corrections are a few pixels of glass contact, never a teleport; TestTitanBodiesStayInFrame: 120 s of sweeps with corner startle bolts, every spine point of every titan and shark inside the canvas (worst margin 3.0 px); spawnPod lays its chain inside the canvas; TestTitanSpineNeverFolds/TestTimeLapse judge open-water bends only — a glass graze is pinned contact, not a whip |
| 84 | The big whites still turned like clock hands: with the eye parked at x+20 and the tail at x-20, the tail must stay on y=0 while the eye holds x (no pivot spin); the pod should glide slowly to x+100 (the right corner) and wheel a graceful 180° in the fake Z, arriving clustered at x-100 | v1.1 G67 | root cause was arithmetic: the arc pace (0.8×maxSp) times the window (2.6 s) laid a 46 px path for a 448 px body — a 15 px radius, the head effectively pivoting in place. The turn is now a true convoy arc: TitanTurnWindow 8.0 s, radius TitanTurnRadiusFrac 0.25×bodyLen, pace v = πR/window (never below 0.9× cruise) — the head travels a quarter tank through the half circle; the LEADER owns the clock (titanStartConvoyTurn flips all five at once, one clock, five parallel arcs, the cluster holds); the arc curls UP against the surface light by default (radius shrinks to the room, down-curl only when the top is closed) and rides the fake Z (the depth lane lifts 0.30→0.52→0.30 mid-arc); the spine axis hangs off the sweep heading (a startle-bent velocity can never tear the chain), the bend relaxes to 0.18 mid-arc and retightens as it closes, and the chain settles onto the new line through the arc itself (eased k, finished when the arc ends); straight cruise: always-on altitude pull + post-turn climb home keep the pod in the upper water, tail wave halved for big bodies (the trailing tip shimmers, it does not wander off the line); burst chases turn the head at 6.5 rad/s and shave both velocity and acceleration off the face blind side. TestTitanConvoyTurn: on the first arc frame all five turn counters are armed (simultaneity), windowed net motion never stalls (no pivot), the pod arrives within 700 px of its leader; fold judges measure true self-overlap with glass-contact exemption; titan/large-body test worlds run at real 1720×720 proportions (an 800 px tank has no legal geometry for a 448 px body) |
| 85 | Flakes must never sink below the sand boundary (treats stay free); uneaten sinking flakes should REST on top of the sand so diving fish make an eye-catching interaction. And the sand must not read as a ruler line — gentle dunes and channels for realism | v1.1 G68 | one shared deterministic relief curve (contract/sand.go SandRelief: three-sine swell ~±11 px, SandSurfaceY = 0.925H + relief) is used by BOTH sides, so simulation and pixels agree on where the surface is; tickFood now settles each flake exactly on SandSurfaceY(x) − 1.5 px with velocity zeroed (at rest it holds its spot — no drift, no burial; live treats untouched and free); DrawSandBed replaced its flat top quad with a 24 px relief-following strip, speckle rows ride the same curve; TestFlakesRestOnSand (three flakes land on the dunes, never below the surface, hold still for 3 s after landing) + TestSandReliefIsNotFlat (relief spans ≥10 px over the tank width); probe 10-critters.png band: duned surface, no ruler line |
| 86 | The big whites move as one cluster: sweep left→right, a graceful 180° in the imaginary Z axis with gentle slow body motion (2D tank — Z is simulated). While gliding x−100→x+100 they should sometimes ride y+100 (the very top) and sometimes y−60 (down to the lilac home level) so they never feel robotic; sometimes they strike for food progressing on both x and y | v1.1 G69 | the sweep is no longer a rail: each elder draws a personal altitude every 18–40 s — a third of the draws ride high under the surface light, the rest glide the broad water down to TitanAltMax 0.66H (just above the nest level) — and eases toward it at a soft weight; vertical drift is gently damped (0.75) and an overshoot brake turns the glide around before it can settle into the bottom band; members draw independently so the pod breathes instead of marching in lockstep; the 180° convoy arc (G67) already carries the fake-Z body motion and the hunger lunge already strikes on both axes (G41, kept); TestTitanSoftVerticalWander — 120 s fed run: real time spent BOTH above 0.35H and below 0.55H, worst straight-cruise vertical pace 21.8 px/s (gentle); TestTitanFavorsUpperWaterAndHorizon retuned to the new contract (dives happen, bottom-band living does not: ≤30%, arc frames excluded) |
| 87 | The big whites: slow, smooth, generally the rearmost layer, sweeping x−100↔x+100 (rare exceptions only), wandering y+100↔y−60 (the nest level), and their Z-turns are fish-like arcs | v1.1 G70 | already built across G39–G69 and pinned by standing tests, re-verified this turn: rearmost layer — the pod holds the far depth lane (0.30±0.12, driftDepth; TestDepthLanesDrift far-band assert), x-sweep with rare exceptions (convoy arc + hunger lunge 4.5× weight with 240 s per-fish CD and the rare guarded predation — TestTitanHungerLungeBurst, PredationGate), y-wander y+100↔y−60 (personal altitudes [~0.22H, 0.66H=nest level], TestTitanSoftVerticalWander), Z-turns as arcs (TestTitanConvoyTurn); new this turn: a strike suspends the glass arc and delays its start (seekBonus gate) so the burst keeps its full budget on both axes — previously the arc froze an in-flight lunge at 22.9 px/s (probe-confirmed) |
| 88 | The hammerheads roam free and must read WILD: sure dorsal fins, prominent animated gill slits | v1.1 G71 | drawSharkWild replaces drawHammer: a swept predator dorsal (rooted 0.30–0.46 along the spine, raked back 0.075×bodyLen) plus a small secondary ridge behind it, five breathing gill slits behind the head pulsing open/shut at 2.2 rad/s (±22 % spread) drawn as dark strokes against the navy, and the hammer tips now carry hunter eyes — an amber ring around a dark pupil; probe 11-titan.png crop: dorsal silhouette and amber eyes visible at tank scale |
| 89 | The regular fish are good as they are — only their tail fins must sit bone-mounted on the tail, never making circular sweeps | v1.1 G72 | the caudal sweep is narrowed and hard-bound to the last vertebra: swish amplitude 0.28+0.22×speed → 0.20+0.14×speed, fin fan half-angle 0.38→0.30 rad — the fin reads as a wagging blade on a bone joint, a full circular swing is geometrically impossible (sin-bounded ±0.34 rad total); spine laws G63 unchanged and still green (TestTimeLapseEveryEyeLeadsItsMotion) |
| 90 | The lilac fish is the star: her every motion must stay the most realistic and beautiful — and she gains a rare superpower: opening a wormhole portal and passing through it | v1.1 G73 | her motion stack is untouched and protected by the standing Chosen tests (F16–F23); the new power: tickChosenPortal — every 200–380 s (a fresh Chosen passes sooner, ~60 s) she parts the water where she swims (0.9 s door opening), glides in fading (0.6 s), steps out of a second door at least 35 % of the tank away, and fades back in (0.6 s); the renderer draws breathing lilac rings at both doors (DrawPortal, neon stays hers alone — F16) and her body alpha rides PortalFade so the pass reads as magic, never a teleport glitch; runtime-only (never persisted); TestChosenPortalRareAndSound — 400 s run: ≥1 and ≤4 passes, each pass jumps > 0.3W but never out of the tank, pass theater ≤ 3.5 s, no fade without an open door, full 1.0 fade at the crossing; the time-lapse eye-leads law exempts her mid-pass frames (a wormhole step is not swimming) |
| 91 | Raise the sense of realism wherever it can be raised — boldly — but keep the artistic line and the virtual-perfect palette nature cannot offer (G74: floor shadows) | v1.1 G74 | every swimmer now hangs a soft shadow patch on the dunes directly under itself: one mesh, one draw call (draw-call economy held — the profile lesson), matte normal-alpha so the neon stays the Chosen's alone (F16); the occlusion law is frozen and tested — near lane darker than far (0.055→0.150 envelope), a fish near the ceiling casts the thinner penumbra (lift term), night keeps exactly ShadowNightMul 0.45 of the day bite, a flat swimmer stretches long and thin while a diver rounds out (footprint ≤0.45×bodyLen), the Chosen mid-portal projects nothing (Fade channel); TestShadowAlphaLaw/TestShadowRadiiLaw/TestShadowsSitOnTheDunes (the mesh center rides the shared relief curve +3 px); probe pixels (11-titan vs 12-night, sand strip): shadow-dark 10.3 % of strip pixels by day → 6.6 % at night, bed median 191→81 |
| 92 | …(G75: sun caustics on the bed) | v1.1 G75 | the bed catches the light: two angled drifting wave trains interfere into a caustic dapple (Caustic01, squared for sharp nodes, phase-breathing so it never loops visibly) painted as one mesh over four depth rows that ride the shared relief curve and settle into the grain; column edges blend their own samples so the 20 px grid never reads as stripes; full sun by day, CausticNightK 0.15 moon whisper at deep night — a shadow reads as blocked light because it dims the dapple under it (draw order: sand → caustics → shadows); TestCausticFieldIsBoundedAndAlive (bounded 0..1, ≥12 brightness buckets, nodes ≥0.55), TestCausticDrifts (≥half the field moves in 6 s), TestCausticNightLaw (frozen gain endpoints); probe pixels: caustic-lit (>±18 of bed median) 2.9 % of strip pixels by day → 1.6 % at night — present, never loud |
| 93 | …(G76: breathing water — seep columns + gill exhale + batched bubbles) | v1.1 G76 | two seep columns fixed on the dunes (0.17W, 0.79W) release loose bubble clusters every 2.2–5 s — the water's bubble setting modulates the vigor but a physical vent never falls silent (VentFloor 0.30); each hammerhead exhales a puff of micro-bubbles at its gills every 3.5–8 s (an echo of the rendered gill slits, G71); the render side replaced the per-bubble orb calls with ONE mesh — a thin glassy ring plus an off-center glint per bubble (the columns would have made per-bubble calls the loudest layer); TestVentsBreatheFromTheDunes (90 s of silent water: ≥30 risen-bubble hits at the anchors — the VentFloor baseline), TestVentsObeyTheKillSwitch (TANK_EXTRAS=0: zero bubbles on silent water), TestSharksExhaleAtTheGills (45 s: gill-proximate bubble frames), TestBubbleBudgetHolds (2 min at full bubble water, cap 96 never exceeded) |
| 94 | …(G77: the floor feed leaves its mark) | v1.1 G77 | a bite taken off the dune throws its dust: tryEat detects a resting flake (within 2.5 px of the shared surface) and kickSand pops 7 grains that rise and drop back under their own weight (Particle.Grav — sparkles drift at 14 px/s², sand drops at 150) — the payoff of the G68 resting-food interaction; TestFloorFeedingKicksSand (≥4 grains thrown, all settled within 2 s) + TestMidWaterBiteKicksNoSand (a mid-water bite throws nothing — the floor is nowhere near); probe drop step DebugFeedFloor stages the resting-flake composition for the day captures |
| 95 | Another realism round by live observation: timed screenshots over the running tank (day→dusk→night→dawn at the 60 s cycle, plus zooms) hunting whatever breaks the reading — a bubble must read as AIR, hollow, not a solid dot | v1.1 G78 | observation caught the seep-column and gill bubbles collapsing into solid dots at screen scale (8-segment bare ring, R 0.7–2.0 logical); every bubble now carries a faint interior window (α 34 through-water) inside a stronger rim (α 140) plus a larger glint (α 190, ×0.30 R), and the spawn radii gained floors — vents 1.4–2.7, gill breaths 1.1–1.9, ambient 1.3–3.6 — so the rim survives a 0.75× downscale; bubbleParts floors (tiny-bubble outer ≥ 1.1, glint never wider than the body); TestBubbleAnatomyLaw + TestBubbleMeshIsAHollowRing (window fan + 8 ring quads + glint per bubble, batched in ONE mesh) + TestBubbleRadiiReadAtScreenScale (silent-water sources hold their floors); verification screenshots: bubbles read "bright rim, hollow center, glint" by day AND night |
| 96 | …and the hunters must own the stage: the observed pair was barely ever on it, and when traced they sat pinned at the left pane facing INTO the glass for minutes | v1.1 G79 | two stacked causes, both measured: (1) arithmetic — RoamMeanSec 7 s at ~55 px/s is ~300 px of travel, so the pair looped locally: 3-minute x-span [0.00, 0.78]W, the right quarter unpatrolled; shark waypoints now persist ×2.5 and 60 % of draws pull to the opposite half (patrol crossings); (2) structural — constrainForward rebuilds velocity from the body axis every frame, so a wall push ANTI-PARALLEL to the nose changes speed, not direction, and the cruise floor erased the speed change each frame (the G63 keepForward deadlock, wall edition: pair frozen at x=0.00, heading locked at π); the pin now yaws the heading toward the wall tangent when the nose points at a pane inside the glide lookahead (×1.6 turn rate), the way a real shark slides along the glass; TestHuntersCoverTheStage (3 min @1720×720: span visits both quarters, width ≥ 0.70, mid-stage ≥ 50 %) green ×3, five-seed spread healthy; verification screenshots: both hunters on stage 12 s in (left-mid + right), visibly patrolled across the tank 55 s later, none touching the panes, nose-first throughout. Side repair: TestChosenPortalRareAndSound simulated 1200 s while claiming 400 s (60×400 frames × dt 0.05) — its rarity ceiling was calibrated on the wrong clock; the loop now runs a true 400 s (gaps 227–297 s, min-gap law intact) |
| 97 | Another analysis round on the movement itself: fish sometimes glitch — the eyes stay put while the body turns many directions (weird on the Y axis), and smoothness breaks in sudden speed-ups/slow-downs that read like the computer just detected a collision | v1.1 G80 | frame-by-frame probe on a mixed tank measured the two families: (1) STATIONARY SPIN — 13,250 sampled events of near-stationary fish (under half body-length speed) rotating their noses over 3 rad/s, peaking at 88 rad/s during titan startles (the "eyes fixed while the body flails" read: capTurn granted the full 5 rad/s turn rate at any speed above 4 px/s, and at near-zero speed even weak wander noise rotates the velocity — the heading chases it); (2) fixes — the wander noise's authority now rides the fin flow (clamped to 15 % at standstill, full from 30 % cruise: only a coherent target force may turn a slow fish, and those turns read as intention), and the turn gate gained a COHERENCE test (a correction sign that flip-flops is noise and turns a slow fish at 0.35× rate; a committed turn holds its sign and swings free); (3) acceptance — TestNoStationaryNoseWhip: per fish, over any rolling 2 s window spent under half body-length speed, a heading path > 7.5 rad built from > 8 reversals fails (coherent circling — a feeding frenzy around a flake — passes; erratic flailing fails); ×3 stable |
| 98 | …the sudden speed changes: no fish may read like collision-response physics | v1.1 G81 | the same probe caught single-frame speed changes up to 7,335 px/s² with nothing armed (MaxForce is 240); vector dumps named the writers: the speed cap's instant proportional cut (179→115 px/s, the spent-burst snap), the glass bounds zeroing the inbound component (vy −132→0), the titan blind-side shave deleting up to 145 px/s, and the shark cruise floor snapping up. Fixes: the spent-burst allowance DECAYS (3.5/s, steer + steerTitan — the cap follows it down as a glide, the strict maxSpeed invariant intact; a full belly simply never renews the drive), the glass component eases off at 10/s, the titan blind side is REFLECTED forward (speed preserved exactly, nose law instant, lateral drift kept for the staggered formation), the shark floor ramps at 3/s, and the held-treat ring gained ARRIVE (the dash settles instead of overshooting into chained U-turns; real food is still hit at speed). Acceptance — TestSpeedChangesStayPhysical: every fish every frame ≤ 1600 px/s², and ≤ 380 px/s² when nothing is armed (no scare, strike, arc, lunge, startle residue; wall contact exempt — the glass ease is by design); ×3 stable; the earlier fixed kinks (timelapse tail-first at the glass, convoy scatter, treat/school/shelter timing) all re-green under the new laws |
| 99 | Focus on the big whites: ponderous bodies like the alien flora — no weird Y-axis turns around the eye, no glitch, no fast in-place moves; instant-acceleration reflexes ONLY for live food and cluster alignment; alignment-prone in general; aggression only at live food, plus the rare starving hunt | v1.1 G82 | the probe measured the residual sins: cruise \|vy\| p95 20.7 px/s — the same size as the whole cruise (the Y-weird read: a sustained post-arc climb ran 16 px/s), and ten random direction darts in four hungry minutes with NO food in sight plus dead-flake dives (the in-place fast moves). Fixes: the lunge is LIVING-FOOD-ONLY (treats; the random dart path and the flake targeting are deleted — TestTitansStrikeOnlyLivingFood: 60 s beside a dead flake, zero strikes, cruise-bound speed; a live worm arms the dive); a LEVEL-FLIGHT envelope caps \|vy\| at 55 % of cruise outside strikes (TestTitanGlidesLevelAndSteady: settled cruise max 12 px/s vertical, no single-frame heading jump over 0.35 rad — with a post-burst grace window); the vertical drift bleeds at TitanVyDamp 1.4 (relaxed far from the drawn altitude so the climb keeps momentum — full damp everywhere turned the sweep into a one-height rail); cruise seed 0.30→0.26; the formation slot is alignment-first (weight 0.8→1.05) and a member blown > 90 px out of line earns the ONE cruise reflex there is (TitanSlotReflex ×1.25); the rare starving hunt is unchanged (TestPredationGate green) |
| 100 | …and the first strike must SHOCK the viewer: the school parts at once, then settles back to normal | v1.1 G83 | strikeShock fires on every titan strike — the live-treat dive, the hunt's opening and the swallow (the swallow reaches further, StrikeScareR2): every small fish inside the radius takes a FLINCH (heading snaps up to 1.1 rad toward open water), a real 55 px/s kick and a decaying startle impulse, so the water visibly OPENS within half a second; a soft pressure ring (one batched mesh, pale water-glass, easing out to ~156 px, gone in 0.65 s, normal alpha — the neon stays the Chosen's) rides both doors of the moment; TestTheStrikeShocksTheSchool: ring born with the dive and dead within its lifetime, ≥ 3 small fish pushed ≥ 30 px outward 0.6 s after the strike, all back under 1.3× cruise 8 s later; the fresh-startle flinch is exempt from the per-frame envelopes and the eye-leads law (a C-start is one beat, judged after it lands); probe 17/18-strike captures bracket the moment |
| 101 | The big whites still read stiff; they must be far softer and heavier — fast ONLY in the instant strike. And at first launch they act fast/odd, settling only later | v1.1 G84 | the opening probe named the opening sin: spawnPod never set altY/altT, so the unset altY=0 pulled the whole pod toward the SURFACE for the first ~12 s — exactly the "fast and weird at first, settles later" read. Members are now born ON their formation slot (no opening shuffle) at the altitude they hold, with the first draw a while out; the level-flight \|vy\| clamp became a 6/s EASE (the instant cut was itself the last stiff edge); the heading ease slowed (2.5→2.0) and the nest-rim heading shift — an instant ±π/2 teleport the big bodies wore as a twitch — is now rate-limited to 1.2 rad/s; TestTitanOpensSettled: the first 30 s ride the same smoothness budget as settled cruise (\|vy\| ≤ 12, no single-frame heading jump over 0.35 rad); two stuck-modes found and killed on the way: the slot reflex rode seekBonus and BLOCKED the convoy's edge trigger (escorts grinding the pane and sinking — the reflex now answers horizontal lag only, since the members' independent altitude draws ARE the breathing, and stands down near the glass), and the turn anti-spam lock (68 s) outlived the slower sweep — a leader could reach the glass still locked (the lock now yields when pressed deep into the margin); a pinned glass hunt also breaks off when the prey hugs the pane |
| 102 | …and the elders must not approach the sand: cruise above the lilac's nest level, descending only rarely | v1.1 G85 | the altitude law moved up: four draws in ten ride high [~0.26, 0.32H], the rest glide the broad water to TitanAltMax 0.50 (was 0.66), one draw in ten is the rare dip to TitanAltDip 0.62 (visiting the nest level, never living there); the down-curl arc floor sits at the nest level (the old 0.74H bound let every closed-top turn dip deep); the water itself gained a FLOOR — past the nest line the climb back escalates with depth (a strike or a hunt may cross; the cruise never lives there); convoy geometry re-derived for the higher band: the turn DIRECTION is decided once by the leader's room (mixed per-member directions turned the pod into a scattered fan) while each member's radius still fits its own water; TestTitansKeepTheUpperWater: over 4 fed minutes ≤ 12 % of frames below the nest line, ZERO frames on the sand side (> 0.72H); TestTitanConvoyTurn green ×3 (the scattered-fan and glass-grind regressions both closed); TestTitanSoftVerticalWander retuned to the new band (deep = the rare dips below 0.50H) |
